{
  "compilerOptions": {
//    "types": ["vite/client"],
//    "module": "esnext",
//    "target": "exnext",
    "downlevelIteration": true,//允许 TS编译器在将代码编译为较旧版本的 JavaScript 时，正确地处理ES2015及以上版本中引入的迭代协议。
    "esModuleInterop": true,
    //允许默认导入CommonJS模块
    "baseUrl": "./",
    // 这里需要配置
    "paths": {
      "@/*": [
        "./src/*"
      ]
      // 这里需要配置
    }
    // 如果baseUrl设置为'src',那paths就应该配置为{"@/*": "./*"},如下：
    // "baseUrl": "src",
    // "paths": {
    //  "@/*": ["./*"]
    // }
  },
  //使用 import.meta.dev需要配置下面两排
//  "--module": "es2022",
//  "moduleResolution": "Node",
// "typeRoots": ["./node_modules/@types", "./src/types"],
  "include": [
    "types/index.d.ts",
    "src/types/reverseImgList.d.ts",
    "src/**/*.ts",
    "src/**/*.d.ts",
    "src/**/*.tsx",
    "src/**/*.vue"
  ],
  "exclude": [
    "node_modules"
  ],
  "files": [],
  "references": [
    {
      "path": "./tsconfig.node.json"
    },
    {
      "path": "./tsconfig.app.json"
    }
  ]
}